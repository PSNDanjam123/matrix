#pragma once
#include "./Types.h"
using namespace Jim::Core::Types;

namespace Jim::Core::Math {
	extern const unit PI;
	extern const unit E;

    extern unit degToRad(unit deg);
    extern unit radToDeg(unit rad);

}
